
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
} from "@/components/ui/form";
import { Switch } from "@/components/ui/switch";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { UseFormReturn } from "react-hook-form";
import { EmailSecuritySettings } from "@/types/emailSecurity";

interface AIEmailFilteringSectionProps {
  form: UseFormReturn<EmailSecuritySettings>;
}

const AIEmailFilteringSection = ({ form }: AIEmailFilteringSectionProps) => {
  return (
    <Form {...form}>
      <div className="space-y-6">
        <h3 className="text-lg font-medium">AI-Powered Email Filtering & DLP Configuration</h3>
        <div className="space-y-6">
          <FormField
            control={form.control}
            name="aiDlpEnabled"
            render={({ field }) => (
              <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                <div className="space-y-0.5">
                  <FormLabel>Enable AI-Powered DLP Email Scanning</FormLabel>
                  <FormDescription>
                    Use AI to scan email content for data loss prevention
                  </FormDescription>
                </div>
                <FormControl>
                  <Switch
                    checked={field.value}
                    onCheckedChange={field.onChange}
                  />
                </FormControl>
              </FormItem>
            )}
          />

          <FormField
            control={form.control}
            name="detectionSensitivity"
            render={({ field }) => (
              <FormItem>
                <FormLabel>Detection Sensitivity Level</FormLabel>
                <Select
                  onValueChange={field.onChange}
                  defaultValue={field.value}
                >
                  <FormControl>
                    <SelectTrigger>
                      <SelectValue placeholder="Select sensitivity level" />
                    </SelectTrigger>
                  </FormControl>
                  <SelectContent>
                    <SelectItem value="low">Low</SelectItem>
                    <SelectItem value="medium">Medium</SelectItem>
                    <SelectItem value="high">High</SelectItem>
                  </SelectContent>
                </Select>
                <FormDescription>
                  Set how sensitive the AI should be when detecting potential issues
                </FormDescription>
              </FormItem>
            )}
          />

          <div className="space-y-3">
            <h4 className="text-sm font-medium">Types of Data to Monitor:</h4>
            
            <FormField
              control={form.control}
              name="monitorPii"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Personally Identifiable Information (PII)</FormLabel>
                    <FormDescription>
                      Social security numbers, addresses, personal contact details
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="monitorFinancial"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Financial Information</FormLabel>
                    <FormDescription>
                      Credit cards, bank accounts, financial records
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="monitorIntellectualProperty"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Intellectual Property & Company Secrets</FormLabel>
                    <FormDescription>
                      Proprietary information, trade secrets, source code
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="monitorCustomerData"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Customer or Employee Data</FormLabel>
                    <FormDescription>
                      Customer records, employee information, contact details
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="monitorHarmfulLanguage"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Harmful or Defamatory Language</FormLabel>
                    <FormDescription>
                      Offensive content, threats, harassment, defamation
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="monitorRegulatoryViolations"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center justify-between rounded-lg border p-3">
                  <div className="space-y-0.5">
                    <FormLabel>Regulatory Violations</FormLabel>
                    <FormDescription>
                      GDPR, HIPAA, SOC2, and other compliance violations
                    </FormDescription>
                  </div>
                  <FormControl>
                    <Switch
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                </FormItem>
              )}
            />
          </div>
        </div>
      </div>
    </Form>
  );
};

export default AIEmailFilteringSection;
