
// This file is now just a re-export from the new structure
// to maintain backward compatibility
import { mockLicenseData, licenseSummary, licenseBreakdown } from './data';

export { mockLicenseData, licenseSummary, licenseBreakdown };
